export { default as extend } from './extend';

// Note: the Cancel button in core's "Reset extension settings" modal is styled
// via CSS (resources/less/admin.less) instead of a runtime DOM observer.
